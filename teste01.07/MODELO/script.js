function contar() {
    var inicio = document.getElementById('inicio')    
    var fim = document.getElementById('fim')
    var passo = document.getElementById('passo')

    if (Number(inicio.value.length) == 0 || Number(fim.value.lenth) == 0 || Number(passo.value.length) == 0)
        window.alert('[ERRO] Verifique os números e insira novamente!')
    else {
        

    }






}